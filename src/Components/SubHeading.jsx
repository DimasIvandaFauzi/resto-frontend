const SubHeading = ({ childern, ...props }) => {
  return <span className={`font-jakarta text-sm text-primary tracking-tighter font-bold ${props.className}`}>HALAMAN</span>;
};
export default SubHeading;
